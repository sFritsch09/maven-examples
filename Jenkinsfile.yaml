pipeline:
  agent:
    any:
  tools:
    maven: 'Maven'
  stages:
    - stage: 'Compile stage'
      steps:
        - sh "mvn -version"
        - git 'https://github.com/sFritsch09/maven-examples.git'
        - sh "cd java-web-project"
        - sh "mvn clean compile"

    - stage: 'Testing stage'
      steps:
        - sh "mvn test"
    - stage: 'Final Jenkins YAML Pipeline Stage'
      steps:
        - sh "echo 'Jenkins YAML Pipeline Complete'"
